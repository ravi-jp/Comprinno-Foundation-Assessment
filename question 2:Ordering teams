#include<iostream>
using namespace std;

void ordering()
{ 
//reading the skills of each person in the group
int a[3],b[3],c[3];
int s1=0,s2=0,s3=0;
  for(i=0;i<=2;i++)
  {
  cin>>a[i];
  s1+=a[i];//aggrigating the skills to obtain total
  }
  for(i=0;i<=2;i++)
     {
        cin>>b[i];
        s2+=b[i];
      }
      for(i=0;i<=2;i++)
     {
        cin>>c[i];
        s3+=c[i];
      }
      /*checking the order exists
      logic is:As stated in question reordering is allowed to make exact order.
              The condition says ,i+1th person must not less than previous person and must greater in any one of the skills.
              Therefore,i+1 person has high aggregate value coimpared to i th person*/
     if(s1==s2||s2==s3||s3==s1)
     {
        cout<<"no";//if any person aggregate equals to other person then the given coditioned oreder can't exist
       }
       else cout<<"yes";
 
}//closing of the function
      
     
    

void main()
{
int t;
cin>>t;
for(int i=0;i<t;i++)
{ 
  ordering();
 }
 }//closing of main()
    
